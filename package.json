{
  "name": "messagequeue",
  "version": "1.0.0",
  "description": "Parent for sub server apps for message queue",
  "main": "index.js",
  "scripts": {
    "start:ClientServ": "node ./ClientServices",
    "start:ClientServ-debug": "node --inspect ./ClientServices",
    "start:FakeServ": "node ./FakeApiServer",
    "postinstall": "cd ./ClientServices && npm install && cd ../FakeApiServer && npm install && cd ../WorkerServer && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.5.3"
  }
}
